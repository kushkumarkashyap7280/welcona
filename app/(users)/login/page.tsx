import { LoginForm } from "@/components/users/LoginForm";

export const metadata = {
  title: "Sign In",
  description: "Sign in to your Welcona account.",
};

export default function LoginPage() {
  return <LoginForm />;
}
